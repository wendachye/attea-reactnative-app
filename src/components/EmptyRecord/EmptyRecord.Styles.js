import {StyleSheet} from 'react-native';

const useStyles = () => {
  return StyleSheet.create({
    container: {
      alignItems: 'center',
      marginTop: 50,
    },
  });
};

export default useStyles;
